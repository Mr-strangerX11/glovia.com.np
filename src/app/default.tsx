// Required by Next.js 14 App Router to suppress the "No default component
// was found for a parallel route" warning when no explicit slot is rendered.
export default function Default() {
  return null;
}
